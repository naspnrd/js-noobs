const person = {
  name: "Neeraj",
  age: 26,
  address: {
    city: "Bengaluru ",
    country: "IN",
  },
};

// const { name, city, age } = person;

// console.log(name, city, age);

// const { name: firstName, city, age = "1" } = person;

// console.log(firstName, city, age);

// const {
//   name,
//   address: { country, city },
//   age,
// } = person;

// console.log(name, country, city, age);

// const { name, ...rest } = person;

// console.log(name, rest);
