// Own Properties -> property defined directly on the object

const myObject = {
  myProp: "Value",
  a: function () {},
};

// Inherited Properties -> property the object inherits from prototype object

myObject.toString();

let arr = ["Neeraj", "Chaudhary"];

console.log(myObject);

function func() {}

// func.

let num = 10;

// num.
let str = "neeraj";
// str.
